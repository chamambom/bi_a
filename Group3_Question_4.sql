SELECT count( * ) as  categories_total_record FROM categories

SELECT count( * ) as  customers_total_record FROM customers

SELECT count( * ) as  departments_total_record FROM departments

SELECT count( * ) as  order_items_total_record FROM order_items

SELECT count( * ) as  orders_total_record FROM orders

SELECT count( * ) as  products_total_record FROM products